import {stableId} from './rng.js';
export function recordMemory(citizen,{kind='episodic',content,source=null,confidence=.7,salience=.5,worldMinute=0}={}){const m={id:stableId('mem',citizen.id,citizen.memories.length,worldMinute,JSON.stringify(content)),kind,content,source,confidence:Math.max(0,Math.min(1,confidence)),salience:Math.max(0,Math.min(1,salience)),createdWorldMinute:worldMinute,lastRecalledWorldMinute:worldMinute};citizen.memories.push(m);if(citizen.memories.length>1500)citizen.memories.splice(0,citizen.memories.length-1500);return m;}
export function decayMemories(citizen,nowMinute){for(const m of citizen.memories){const age=Math.max(0,nowMinute-m.lastRecalledWorldMinute);m.confidence=Math.max(.05,m.confidence-age/5_000_000*(1-m.salience*.8));}citizen.memories=citizen.memories.filter(m=>m.salience>.8||m.confidence>.08);}
